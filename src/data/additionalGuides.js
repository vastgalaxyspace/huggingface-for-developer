const commonSources = [
  { label: 'Hugging Face Transformers Documentation', href: 'https://huggingface.co/docs/transformers' },
  { label: 'vLLM Documentation', href: 'https://docs.vllm.ai/' },
  { label: 'NVIDIA CUDA Documentation', href: 'https://docs.nvidia.com/cuda/' },
  { label: 'InnoAI VRAM Calculator', href: '/gpu/tools/vram-calculator' },
  { label: 'InnoAI GPU Picker', href: '/gpu/tools/gpu-picker' },
];

const guide = ({
  slug,
  title,
  category,
  description,
  takeaways,
  sections,
  faq,
  related,
}) => ({
  slug,
  title,
  description,
  category,
  readTime: '12 min read',
  lastUpdated: '2026-05-13',
  publishedDate: '2026-05-13',
  author: 'Dhiraj',
  reviewedBy: 'InnoAI Technical Review',
  difficulty: category.includes('Advanced') ? 'Advanced' : category.includes('Intermediate') ? 'Intermediate' : 'Beginner',
  qualityVersion: 'v1.1',
  keyTakeaways: takeaways,
  whatYouWillLearn: takeaways,
  sections,
  checklist: [
    'Identify the workload before choosing a runtime or model format.',
    'Check whether the optimization changes quality, latency, memory, or all three.',
    'Measure time to first token, tokens per second, p95 latency, and GPU memory.',
    'Keep a full-precision or baseline run for comparison.',
    'Document hardware, model revision, context length, and batch settings.',
  ],
  faq,
  sources: commonSources,
  related,
  endLinks: [
    { href: '/gpu/tools/vram-calculator', title: 'Estimate VRAM', body: 'Turn model size, precision, and context assumptions into deployment memory estimates.' },
    { href: '/compare', title: 'Compare models', body: 'Review architecture, context, license, and deployment signals side by side.' },
  ],
});

export const additionalGuides = [
  guide({
    slug: 'what-is-vllm',
    title: 'What is vLLM? A Practical Guide for LLM Serving',
    category: 'Beginner Inference',
    description: 'vLLM is an inference engine designed to serve large language models with high throughput, efficient memory use, and production-friendly batching.',
    takeaways: [
      'vLLM is a serving runtime, not a model family.',
      'PagedAttention helps reduce KV cache waste during concurrent serving.',
      'The biggest benefits appear when many requests share the same GPU pool.',
      'Model compatibility, quantization format, and GPU memory still decide feasibility.',
    ],
    sections: [
      { heading: '1. What vLLM does', content: 'vLLM runs transformer language models behind an API server or Python interface. Its job is to accept prompts, schedule requests, manage GPU memory, and generate tokens efficiently. Developers often discover vLLM after a local prototype becomes too slow or too expensive with naive generation loops. Instead of treating each request as an isolated script, vLLM operates like a serving system where batching, memory reuse, and scheduling are first-class concerns.' },
      { heading: '2. Why PagedAttention matters', content: 'The key idea associated with vLLM is PagedAttention. During generation, every active request stores key-value cache tensors. In simple systems, this cache can waste memory because sequences are different lengths and allocations are not flexible. PagedAttention borrows a paging idea from operating systems: it manages cache blocks in smaller units so many requests can share memory more efficiently. This does not make memory free, but it can allow higher concurrency before the GPU runs out of VRAM.' },
      { heading: '3. When vLLM is a good fit', content: 'vLLM is most useful when you need to serve text-generation workloads with multiple concurrent users, long prompts, streaming responses, or OpenAI-compatible APIs. It is a strong candidate for chat applications, retrieval-augmented generation, internal assistants, and batch inference services. If you only run one prompt manually every few minutes, the operational benefits are smaller. If you need stable production throughput, queueing, and better GPU utilization, vLLM becomes much more attractive.' },
      { heading: '4. What vLLM does not solve', content: 'vLLM does not automatically make a model fit on a small GPU. The model weights, KV cache, precision, and runtime overhead still need memory. It also does not guarantee that a quantized model has the same quality as full precision. Developers should avoid treating vLLM as a magic speed switch. It is a runtime that improves serving efficiency when model format, hardware, and workload shape are compatible.' },
      { heading: '5. Hardware planning', content: 'Start by estimating the model weight footprint, then add KV cache for the target context length and concurrency. A 7B or 8B model may fit on a high-end consumer card, while larger models usually need quantization, multiple GPUs, or data-center hardware. The important measurement is not only whether a single prompt runs. Production planning needs p95 latency, tokens per second, time to first token, and maximum concurrent requests before quality of service degrades.' },
      { heading: '6. Deployment workflow', content: 'A practical rollout starts with a baseline model, fixed prompts, and a known GPU. Run the model without heavy tuning, record memory and latency, then enable batching, quantization, or parallelism one change at a time. Keep logs for request length and output length because those are major drivers of cost and latency. When a change improves speed but harms answer quality, keep the baseline result available so the team can make a clear tradeoff.' },
      { heading: '7. How it relates to other tools', content: 'vLLM sits beside tools such as TensorRT-LLM, llama.cpp, Hugging Face Text Generation Inference, and custom Transformers servers. llama.cpp is excellent for GGUF and local workflows. TensorRT-LLM can be powerful when NVIDIA-specific optimization is worth the complexity. vLLM is often a practical middle path because it is flexible, widely adopted, and friendly to OpenAI-compatible application code.' },
      { heading: '8. Practical recommendation', content: 'Use vLLM when you are ready to move from experiments to a real service. Choose it for concurrency, streaming, batching, and memory-aware serving. Do not choose it only because it is popular. First confirm the model is supported, the GPU has enough memory, the desired quantization format is practical, and your evaluation set shows acceptable answer quality under the exact serving configuration.' },
    ],
    faq: [
      { q: 'Is vLLM a model?', a: 'No. vLLM is a serving engine for running compatible language models efficiently.' },
      { q: 'Does vLLM reduce VRAM usage?', a: 'It can reduce KV cache waste during serving, but model weights and active cache still require VRAM.' },
      { q: 'Should I use vLLM for one local model?', a: 'Usually only if you want an API server or plan to test production-like serving behavior.' },
    ],
    related: ['kv-cache-optimization', 'pagedattention-internals', 'tensor-parallelism'],
  }),
  guide({
    slug: 'what-is-quantization',
    title: 'What is Quantization? FP16, INT8, INT4, GGUF, AWQ, and GPTQ',
    category: 'Beginner Optimization',
    description: 'Quantization reduces model memory by storing weights with fewer bits, but it changes the balance between quality, speed, compatibility, and deployment simplicity.',
    takeaways: [
      'Quantization is a deployment tradeoff, not a universal upgrade.',
      'FP16 or BF16 should be the quality baseline before testing smaller formats.',
      'GGUF, AWQ, and GPTQ target different runtimes and workflows.',
      'Always test representative prompts after changing precision.',
    ],
    sections: [
      { heading: '1. The basic idea', content: 'Neural network weights are numbers. Full training often uses higher precision, while inference can frequently use lower precision without unacceptable quality loss. Quantization stores weights in fewer bits, such as 8-bit or 4-bit, so the model uses less memory. Less memory can make a larger model fit on available hardware, reduce bandwidth pressure, and sometimes improve speed. The tradeoff is that lower precision can introduce approximation error.' },
      { heading: '2. Why developers use it', content: 'The most common reason is VRAM. A model that does not fit in FP16 may fit in INT8 or INT4. This can turn a multi-GPU deployment into a single-GPU deployment or make local testing possible on consumer hardware. Quantization can also lower cloud cost because smaller GPUs become viable. However, the real benefit depends on runtime support; a format that fits in memory but runs slowly is not a successful deployment.' },
      { heading: '3. FP16 and BF16 baselines', content: 'Before quantizing, create a baseline with FP16 or BF16 if hardware supports it. This gives the team a reference for answer quality, latency, and memory. Without a baseline, it is hard to know whether a quantized model is failing because of the base model, prompt, runtime, or precision. BF16 can be more numerically stable on supported hardware, while FP16 is widely used and easy to reason about for memory estimates.' },
      { heading: '4. INT8 and 4-bit tradeoffs', content: 'INT8 often preserves quality well and is a conservative first step for memory reduction. Four-bit formats can provide dramatic memory savings, but they deserve more careful testing. Some tasks tolerate 4-bit quantization well, especially casual chat or extraction. Other tasks, such as reasoning, code generation, tool calling, and strict JSON output, can expose subtle failures. The safest method is to compare outputs on the exact prompts your application will use.' },
      { heading: '5. GGUF, AWQ, and GPTQ', content: 'GGUF is closely associated with llama.cpp and local inference workflows. It is popular for desktop, CPU-assisted, and small-server deployments. AWQ is commonly used for efficient GPU inference and can preserve quality well for many transformer models. GPTQ is another established post-training quantization approach with broad model availability. The right choice depends on runtime, kernels, hardware, and whether a trusted quantized variant already exists.' },
      { heading: '6. Quality testing', content: 'A practical test set should include normal prompts, edge cases, long-context prompts, structured output, refusal-sensitive prompts, and examples where the model previously made mistakes. Score both correctness and formatting. If the quantized model is almost as good but much cheaper to serve, it may be the right production choice. If it saves memory but breaks high-value tasks, keep it for low-risk routing or choose a less aggressive format.' },
      { heading: '7. Operational risks', content: 'Quantized variants can lag behind base-model releases, may have unclear provenance, and sometimes use settings that are not obvious from the filename. Teams should document the exact repository, revision, quantization method, calibration assumptions, runtime, and GPU. This matters for debugging because changing any one of those can change behavior. Treat the quantized artifact as a deployment dependency, not just a compressed copy.' },
      { heading: '8. Recommendation', content: 'Use quantization when memory or cost blocks deployment, but preserve a baseline. Start with INT8 when quality is critical, test 4-bit when fit or cost is the main constraint, and choose GGUF, AWQ, or GPTQ based on the runtime you actually plan to use. Never approve a quantized model only because the VRAM estimate looks attractive.' },
    ],
    faq: [
      { q: 'Does quantization always make inference faster?', a: 'No. Speed depends on kernels, runtime, hardware, batch size, and memory bandwidth.' },
      { q: 'Is 4-bit good enough for production?', a: 'Sometimes, but it must be tested on real prompts and output requirements.' },
      { q: 'Which format should local users start with?', a: 'GGUF is often the simplest local starting point because llama.cpp tooling is mature.' },
    ],
    related: ['how-gguf-works', 'kv-cache-optimization', 'choose-ai-model-by-gpu-budget'],
  }),
  guide({
    slug: 'how-gguf-works',
    title: 'How GGUF Works for Local LLM Deployment',
    category: 'Beginner Optimization',
    description: 'GGUF is a model file format used heavily with llama.cpp-style local inference, especially for quantized models on consumer hardware.',
    takeaways: [
      'GGUF packages model tensors and metadata for llama.cpp-compatible runtimes.',
      'It is popular because it makes local deployment approachable.',
      'Quantization level, context length, and offload settings determine real performance.',
      'GGUF is not the same thing as AWQ or GPTQ GPU serving.',
    ],
    sections: [
      { heading: '1. What GGUF is', content: 'GGUF is a file format that stores model tensors and metadata in a way llama.cpp-compatible runtimes can load. Developers usually encounter it when downloading quantized open models for local inference. A GGUF file can represent different quantization levels, so filenames often include labels such as Q4, Q5, Q8, or similar variants. The format is part of a practical local deployment ecosystem rather than a model architecture.' },
      { heading: '2. Why it became popular', content: 'GGUF became popular because it makes local LLM testing accessible. Users can download one file, choose a runtime, and run a model on CPU, GPU, or a mix of both depending on hardware. This is different from many server-style deployments where model shards, tokenizer files, config files, and runtime settings must be coordinated. For developers trying models on laptops or desktops, that simplicity matters.' },
      { heading: '3. Quantization labels', content: 'A GGUF filename often tells you the quantization family. Lower-bit variants usually use less memory but may lose quality. Higher-bit variants use more memory but can be closer to the original model. The best variant depends on hardware and task sensitivity. A Q4 model might be good enough for casual chat, while code, reasoning, and precise extraction may benefit from Q5, Q6, Q8, or a non-quantized baseline if available.' },
      { heading: '4. CPU and GPU offload', content: 'llama.cpp-style runtimes can place some layers on GPU and keep the rest on CPU. This makes GGUF useful when a model almost fits but not entirely. Offload can improve speed, but PCIe transfer, CPU memory bandwidth, and layer placement matter. A model that technically runs with partial offload may still be too slow for interactive use. Always measure tokens per second and time to first token.' },
      { heading: '5. Context length and KV cache', content: 'Even with GGUF, the KV cache can dominate memory for long prompts or concurrent sessions. Increasing context length is not free. Developers often focus on the model file size and forget that runtime memory grows as the conversation grows. If a model crashes or slows after longer usage, reduce context length, use a smaller quantization, offload more carefully, or choose a smaller model.' },
      { heading: '6. When GGUF is the right format', content: 'GGUF is a strong choice for local assistants, offline prototypes, privacy-sensitive desktop workflows, edge experiments, and simple internal tools where llama.cpp support is enough. It is less ideal when the production target is a high-throughput GPU API with many concurrent users. In those cases, AWQ, GPTQ, FP16, or runtime-specific formats may be more appropriate.' },
      { heading: '7. Evaluation workflow', content: 'Start with a model family and size that matches your hardware. Test two or three quantization levels using the same prompts. Record memory, speed, answer quality, and formatting reliability. If a smaller GGUF variant fails important tasks, do not assume prompt tweaks will fix it. Try a larger quantization level or a smaller base model at higher precision.' },
      { heading: '8. Practical recommendation', content: 'Use GGUF when deployment simplicity and local control matter most. Choose the highest quantization level your hardware can handle comfortably, then reduce only if speed or memory is unacceptable. Keep notes about runtime version, context length, thread settings, GPU layers, and model revision so future results can be reproduced.' },
    ],
    faq: [
      { q: 'Is GGUF only for CPU?', a: 'No. GGUF runtimes can use CPU, GPU, or partial GPU offload depending on hardware and settings.' },
      { q: 'Is GGUF better than AWQ?', a: 'They serve different workflows. GGUF is common for llama.cpp/local use, while AWQ is common for GPU serving.' },
      { q: 'Can GGUF handle long context?', a: 'Yes, but KV cache memory still grows with context length and must be planned.' },
    ],
    related: ['what-is-quantization', 'kv-cache-optimization', 'what-is-vllm'],
  }),
  guide({
    slug: 'tensor-parallelism',
    title: 'Tensor Parallelism for LLM Inference',
    category: 'Intermediate Inference',
    description: 'Tensor parallelism splits model computation across multiple GPUs so larger models or higher throughput deployments can become practical.',
    takeaways: [
      'Tensor parallelism is used when one GPU cannot comfortably hold or serve the model.',
      'It introduces communication overhead between GPUs.',
      'Fast interconnects matter for performance.',
      'It should be tested against quantization and smaller-model alternatives.',
    ],
    sections: [
      { heading: '1. What tensor parallelism means', content: 'Tensor parallelism splits parts of transformer computation across multiple GPUs. Instead of putting a full copy of the model on every GPU, layers or matrix operations are partitioned so each device handles a slice. This can make models practical when their weights, KV cache, or throughput target exceed a single GPU. It is common in large-model inference, but it adds coordination cost.' },
      { heading: '2. Why it is used', content: 'The obvious reason is memory. A model with an FP16 footprint above one GPU capacity needs sharding, quantization, offload, or a smaller model. Tensor parallelism can also improve throughput when a single GPU is too slow. However, splitting work means GPUs must communicate partial results. If interconnect bandwidth is poor, the communication overhead can erase much of the benefit.' },
      { heading: '3. How it changes deployment planning', content: 'Single-GPU planning asks whether the model and KV cache fit. Tensor-parallel planning asks whether the model fits across GPUs and whether communication stays acceptable. The number of GPUs, GPU memory, NVLink or PCIe topology, batch size, sequence length, and runtime implementation all matter. The same model can perform differently on four GPUs in one server versus four GPUs spread across slower links.' },
      { heading: '4. When to prefer quantization instead', content: 'If a model barely misses single-GPU memory, quantization may be simpler than tensor parallelism. A strong 8-bit or 4-bit variant can reduce operational complexity and avoid communication overhead. Tensor parallelism becomes more attractive when quality requirements demand higher precision, the model is too large even after conservative quantization, or high throughput justifies multiple GPUs.' },
      { heading: '5. Runtime support', content: 'Runtimes such as vLLM, TensorRT-LLM, and other distributed inference systems can support tensor parallel patterns, but configuration details differ. Developers should read runtime-specific docs and avoid assuming flags are portable. Pay attention to supported architectures, quantization compatibility, maximum context length, and how the runtime handles KV cache across devices.' },
      { heading: '6. Failure modes', content: 'Common problems include out-of-memory errors despite sharding, slow generation due to communication overhead, uneven GPU utilization, unsupported quantized formats, and unexpected latency spikes under concurrency. These issues are easier to diagnose when you log model revision, tensor parallel size, context length, batch settings, GPU type, and interconnect topology.' },
      { heading: '7. Measurement strategy', content: 'Measure baseline single-GPU or quantized performance first if possible. Then test tensor parallel sizes such as two, four, or eight GPUs. Track throughput, latency, memory per GPU, and utilization. Watch for cases where adding GPUs increases throughput but hurts p95 latency. Production systems often need a balance rather than maximum aggregate tokens per second.' },
      { heading: '8. Practical recommendation', content: 'Use tensor parallelism when model quality or throughput justifies multi-GPU complexity. For many teams, the simpler path is a smaller model, quantization, or routing between small and large models. When tensor parallelism is necessary, choose hardware with strong interconnects, keep runtime versions pinned, and test representative prompts before committing spend.' },
    ],
    faq: [
      { q: 'Does tensor parallelism make inference linearly faster?', a: 'Not usually. Communication overhead prevents perfect scaling.' },
      { q: 'Do I need NVLink?', a: 'Not always, but faster interconnects usually improve large-model sharded inference.' },
      { q: 'Is tensor parallelism the same as data parallelism?', a: 'No. Tensor parallelism splits model computation; data parallelism replicates the model for separate batches.' },
    ],
    related: ['what-is-vllm', 'kv-cache-optimization', 'cuda-graph-optimization'],
  }),
  guide({
    slug: 'kv-cache-optimization',
    title: 'KV Cache Optimization for LLM Inference',
    category: 'Intermediate Inference',
    description: 'The KV cache stores attention keys and values during generation, and optimizing it is essential for long context, concurrency, and memory stability.',
    takeaways: [
      'KV cache grows with sequence length, layers, heads, hidden size, precision, and concurrency.',
      'Long context can fail even when model weights fit.',
      'Paged allocation and careful batching reduce memory waste.',
      'Measure cache pressure under realistic prompts, not tiny demos.',
    ],
    sections: [
      { heading: '1. What the KV cache is', content: 'During autoregressive generation, transformer models reuse attention keys and values from previous tokens. The KV cache stores those tensors so the model does not recompute the full prompt at every new token. This is essential for speed, but it consumes memory. The larger the model, context, and concurrency, the more important cache planning becomes.' },
      { heading: '2. Why it surprises teams', content: 'Many teams estimate memory from model weights alone. The model loads successfully, a short prompt works, and then production fails when users send long documents or multiple sessions run together. The KV cache grows with prompt length and generated length. A model that fits at 2K tokens may not fit at 32K tokens with the same batch and concurrency.' },
      { heading: '3. Main drivers', content: 'KV cache size depends on number of layers, number of key-value heads, head dimension, precision, active tokens, and active sequences. Grouped-query attention can reduce cache size compared with full multi-head attention. Lower precision cache can reduce memory, but quality and runtime support must be checked. Runtime allocation strategy also matters because fragmented or over-reserved cache wastes capacity.' },
      { heading: '4. Optimization methods', content: 'Common methods include paged cache allocation, continuous batching, lower-precision cache, context limits, prompt truncation, retrieval chunking, prefix caching, and routing long-context requests to specialized models. Each method changes behavior. Truncation may remove important evidence, while lower-precision cache may affect output quality. Optimization should be tied to user-facing requirements.' },
      { heading: '5. Relationship to vLLM', content: 'vLLM popularized PagedAttention as a practical way to manage KV cache blocks efficiently during serving. It helps reduce waste when many requests of different lengths are active. This is valuable for production APIs where prompts and generation lengths vary. It does not eliminate cache memory; it manages it more intelligently.' },
      { heading: '6. Testing realistic prompts', content: 'A good test includes short prompts, average prompts, worst-case long prompts, and concurrent sessions. Record memory before generation, after prefill, and during decoding. Measure time to first token separately from tokens per second because long prompts stress prefill heavily. If a system only works on toy prompts, it is not ready for production.' },
      { heading: '7. Product decisions', content: 'KV cache optimization often becomes a product decision. You may cap document size, summarize earlier turns, retrieve fewer chunks, or route long prompts to a larger GPU. These choices affect user experience and cost. Make the limits explicit in application design instead of discovering them through out-of-memory errors.' },
      { heading: '8. Practical recommendation', content: 'Treat KV cache as part of the deployment budget from day one. Estimate it for expected context and concurrency, then confirm with runtime measurements. If memory is tight, consider smaller models, lower precision, shorter context, smarter retrieval, or paged cache runtimes before adding expensive GPUs.' },
    ],
    faq: [
      { q: 'Why does memory grow during generation?', a: 'The runtime stores attention keys and values for active tokens so future tokens can reuse them.' },
      { q: 'Does quantizing weights reduce KV cache?', a: 'Not necessarily. Cache precision is separate and depends on runtime support.' },
      { q: 'What is prefill?', a: 'Prefill processes the input prompt before token-by-token decoding begins.' },
    ],
    related: ['what-is-vllm', 'pagedattention-internals', 'flashattention'],
  }),
  guide({
    slug: 'flashattention',
    title: 'FlashAttention Explained for LLM Developers',
    category: 'Intermediate Performance',
    description: 'FlashAttention improves attention performance by reducing memory traffic and using GPU-friendly computation patterns.',
    takeaways: [
      'FlashAttention is an attention kernel optimization, not a model.',
      'It reduces memory movement, which is often the attention bottleneck.',
      'Benefits depend on GPU, sequence length, precision, and runtime support.',
      'It should be evaluated with real prompt lengths and batch sizes.',
    ],
    sections: [
      { heading: '1. The attention bottleneck', content: 'Transformer attention can be expensive because it compares tokens with other tokens and moves large intermediate tensors through memory. For long sequences, memory traffic becomes a major bottleneck. FlashAttention addresses this by changing how attention is computed so the GPU spends less time reading and writing huge matrices to slower memory.' },
      { heading: '2. What FlashAttention changes', content: 'Traditional attention implementations can materialize large attention matrices. FlashAttention uses tiling and recomputation strategies so more work happens in fast on-chip memory. The output should match the mathematical intent of attention while using memory more efficiently. Developers do not usually call the kernel directly; they benefit when frameworks and runtimes use it under the hood.' },
      { heading: '3. Where it helps most', content: 'The benefit is often strongest with longer context lengths, compatible GPUs, and precision modes supported by optimized kernels. Short prompts may not show dramatic gains because overheads dominate. Long prompts, larger batches, and production serving workloads are more likely to expose the memory-bandwidth savings. Always test the sequence lengths your application actually uses.' },
      { heading: '4. Runtime compatibility', content: 'FlashAttention support depends on model architecture, attention pattern, GPU generation, framework version, and installed kernels. Some models use sliding window attention, grouped-query attention, or other variants that change support requirements. If a runtime silently falls back to a slower kernel, performance assumptions can be wrong. Confirm logs or profiling output when possible.' },
      { heading: '5. Relationship to KV cache', content: 'FlashAttention improves attention computation, while KV cache optimization manages stored keys and values during generation. They are related but not identical. A serving stack may use FlashAttention for efficient prefill and paged KV cache for efficient decoding under concurrency. Long-context systems often need both types of optimization.' },
      { heading: '6. Measurement strategy', content: 'Measure time to first token, full response latency, GPU memory, and throughput before and after enabling FlashAttention. Use fixed model revision, precision, prompt length, and batch size. If results are inconsistent, check whether the optimized kernel is actually active. Small benchmark scripts can mislead if they do not match production request shapes.' },
      { heading: '7. Common mistakes', content: 'A common mistake is assuming FlashAttention makes any model cheap to run. It helps with a specific bottleneck, but model weights, KV cache, sampling, network overhead, and application code still matter. Another mistake is comparing different model versions or prompt lengths while attributing all performance differences to the attention kernel.' },
      { heading: '8. Practical recommendation', content: 'Use FlashAttention when your runtime supports it and attention is a bottleneck, especially for longer contexts. Treat it as one optimization in a stack that may include vLLM, PagedAttention, quantization, batching, and CUDA graphs. Keep a clear baseline so you can prove the gain on your own workload.' },
    ],
    faq: [
      { q: 'Is FlashAttention only for training?', a: 'No. It can help both training and inference depending on runtime support.' },
      { q: 'Does it reduce model size?', a: 'No. It changes attention computation, not the number of model parameters.' },
      { q: 'Why do long prompts benefit more?', a: 'Attention memory traffic grows with sequence length, so efficient kernels matter more.' },
    ],
    related: ['kv-cache-optimization', 'pagedattention-internals', 'cuda-graph-optimization'],
  }),
  guide({
    slug: 'pagedattention-internals',
    title: 'PagedAttention Internals: Why KV Cache Paging Matters',
    category: 'Advanced Inference',
    description: 'PagedAttention manages KV cache memory in blocks so LLM serving systems can handle variable-length requests with less waste.',
    takeaways: [
      'PagedAttention addresses KV cache allocation waste.',
      'It is especially useful for concurrent serving with variable sequence lengths.',
      'It improves memory utilization but does not remove memory limits.',
      'Understanding it helps explain why vLLM can serve more requests per GPU.',
    ],
    sections: [
      { heading: '1. The allocation problem', content: 'LLM serving systems handle requests with different prompt lengths and generation lengths. If the runtime reserves large contiguous cache regions for every request, memory can be wasted or fragmented. Some requests finish early, some grow longer than expected, and some need more cache blocks during decoding. PagedAttention solves this by managing KV cache in smaller blocks.' },
      { heading: '2. Operating-system analogy', content: 'The name comes from memory paging ideas in operating systems. Instead of requiring one large continuous allocation, the runtime tracks blocks and maps logical token positions to physical cache storage. This lets active sequences grow and shrink more flexibly. The analogy is not perfect, but it helps explain why paging improves utilization under mixed workloads.' },
      { heading: '3. Why variable length matters', content: 'If every request had the same prompt length and output length, cache planning would be easier. Real applications are not like that. One user asks a short question, another sends a long document, and a retrieval system inserts several chunks. Variable length creates unused space in naive allocation schemes. PagedAttention reduces that waste and can allow more simultaneous sequences before out-of-memory.' },
      { heading: '4. Throughput implications', content: 'Better cache utilization can improve throughput because the same GPU can keep more useful work active. However, throughput still depends on model size, batch scheduling, attention kernels, precision, sampling, and hardware. PagedAttention is one reason vLLM can perform well, but it is not the only part of a serving system.' },
      { heading: '5. Limits and tradeoffs', content: 'Paging adds metadata and scheduling complexity. It also cannot overcome fundamental memory requirements. If the model weights and active cache exceed VRAM, paging cannot make the workload fit. Developers should view it as an efficiency mechanism that improves how memory is used, not as a replacement for capacity planning.' },
      { heading: '6. Prefix and prompt reuse', content: 'Some serving systems can reuse prefix cache when multiple requests share the same beginning, such as system prompts or common retrieval templates. Paged cache management can work alongside such techniques. The product implication is simple: repeated prompt structure can be valuable, but it must be measured because user-specific context may reduce reuse.' },
      { heading: '7. Debugging cache pressure', content: 'Symptoms of cache pressure include out-of-memory errors under concurrency, sudden latency spikes, queue growth, or lower-than-expected throughput for long prompts. Collect prompt length, output length, active sequence count, and memory metrics. Without those logs, teams often blame the model when the actual problem is serving configuration.' },
      { heading: '8. Practical recommendation', content: 'Use PagedAttention-capable runtimes when concurrency and varied sequence lengths are expected. Pair it with explicit context limits, realistic load tests, and memory estimates. If your workload is single-user local inference, the benefits may be less visible; if it is a production API, they can be decisive.' },
    ],
    faq: [
      { q: 'Is PagedAttention the same as FlashAttention?', a: 'No. PagedAttention manages KV cache allocation; FlashAttention optimizes attention computation.' },
      { q: 'Does it help short prompts?', a: 'It may, but the largest benefits usually appear with concurrency and variable lengths.' },
      { q: 'Can it prevent every OOM?', a: 'No. It reduces waste but cannot exceed physical VRAM limits.' },
    ],
    related: ['what-is-vllm', 'kv-cache-optimization', 'flashattention'],
  }),
  guide({
    slug: 'cuda-graph-optimization',
    title: 'CUDA Graph Optimization for LLM Inference',
    category: 'Advanced Performance',
    description: 'CUDA graphs can reduce CPU launch overhead by capturing repeated GPU work, but they require stable shapes and careful runtime support.',
    takeaways: [
      'CUDA graphs reduce repeated kernel launch overhead.',
      'They work best when execution shapes are stable.',
      'Dynamic request patterns can reduce their effectiveness.',
      'Measure latency improvements separately from memory or quality changes.',
    ],
    sections: [
      { heading: '1. The launch overhead problem', content: 'GPU workloads are made of many kernel launches. For small or repeated operations, CPU-side launch overhead can become visible. LLM inference includes repeated decoding steps, so reducing launch overhead can improve latency. CUDA graphs let a runtime capture a sequence of GPU operations and replay it more efficiently when shapes and execution patterns are compatible.' },
      { heading: '2. What CUDA graphs capture', content: 'A CUDA graph represents a recorded set of GPU operations and dependencies. Instead of issuing every operation individually each time, the runtime can replay the captured graph. This can reduce overhead and improve consistency. The challenge is that graph capture prefers stable tensor shapes, memory addresses, and control flow. Highly dynamic serving can make capture more complicated.' },
      { heading: '3. Where LLM inference benefits', content: 'Decode loops often repeat similar operations token after token. If the runtime can stabilize shapes through batching or padding, graph replay can help. The benefit may show up as lower per-token latency or smoother p95 behavior. Prefill for long prompts may be less graph-friendly because prompt lengths vary widely, though runtime-specific techniques can still help.' },
      { heading: '4. Compatibility issues', content: 'CUDA graph support depends on the inference engine, model architecture, quantization, GPU, driver, and framework version. Some features may disable graph capture or force fallback paths. Developers should check runtime logs and benchmark both enabled and disabled modes. Silent fallback can make teams believe an optimization is active when it is not.' },
      { heading: '5. Shape management', content: 'Stable shapes are the central operational requirement. Serving systems may bucket requests, pad sequences, or use fixed batch sizes to make graphs reusable. Those choices can improve GPU efficiency but may waste some compute. The best configuration depends on traffic patterns. A public chat product and a nightly batch job usually need different settings.' },
      { heading: '6. Measuring impact', content: 'Measure time to first token, per-token decode latency, p95 latency, and CPU utilization. CUDA graphs primarily target launch overhead, so they should not be credited for changes caused by quantization, cache limits, or model switching. Use the same model, precision, context length, and runtime settings except for the graph option.' },
      { heading: '7. Failure modes', content: 'Common problems include capture errors, excessive padding, increased memory reservation, incompatibility with dynamic shapes, and confusing benchmark results. If graph capture increases memory enough to reduce concurrency, the net result may be negative. Treat it as a production tuning option, not a default assumption.' },
      { heading: '8. Practical recommendation', content: 'Use CUDA graphs after the basic deployment is stable. First choose the model, runtime, precision, and batching strategy. Then test graph capture on representative traffic. Keep rollback simple because graph-related issues can appear only under specific shapes or concurrency levels.' },
    ],
    faq: [
      { q: 'Do CUDA graphs improve quality?', a: 'No. They are a performance optimization and should preserve model outputs.' },
      { q: 'Are they NVIDIA-specific?', a: 'CUDA graphs are part of NVIDIA CUDA; other platforms have different mechanisms.' },
      { q: 'Should beginners tune CUDA graphs first?', a: 'No. Start with model size, precision, runtime, and batching before advanced graph tuning.' },
    ],
    related: ['tensor-parallelism', 'flashattention', 'what-is-vllm'],
  }),
  guide({
    slug: 'moe-routing',
    title: 'MoE Routing Explained for Mixture-of-Experts Models',
    category: 'Advanced Architecture',
    description: 'Mixture-of-experts models activate only part of their parameters per token, which changes capacity, memory, routing, and deployment behavior.',
    takeaways: [
      'MoE models have total parameters and active parameters; both matter.',
      'Routing decides which experts process each token.',
      'MoE can improve capability per active compute but complicates serving.',
      'Memory planning must consider all resident experts, not only active ones.',
    ],
    sections: [
      { heading: '1. What MoE means', content: 'A mixture-of-experts model contains multiple expert feed-forward networks and a router that selects which experts handle each token. Only a subset of experts is active for a given token, so active compute can be much smaller than total parameter count. This is why MoE models can advertise very large total parameters while using fewer active parameters during generation.' },
      { heading: '2. Total vs active parameters', content: 'Total parameters affect storage and memory because the experts generally need to be available to the runtime. Active parameters affect compute per token. Developers should not compare a dense 70B model and an MoE model using only total parameters. The relevant comparison depends on memory capacity, expert placement, active expert count, routing behavior, and runtime support.' },
      { heading: '3. How routing works', content: 'The router scores experts for each token and selects one or more experts. Some architectures use top-k routing, where each token is sent to a small number of experts. Routing quality matters because poor routing can waste expert capacity or hurt output quality. During inference, routing also affects load balance because some experts may become more active than others.' },
      { heading: '4. Deployment implications', content: 'MoE serving can be more complex than dense serving. All experts may need to reside in memory, expert parallelism may be useful, and load balance can affect throughput. A model with low active parameters may still require significant VRAM. Runtime support is especially important; an MoE architecture that is efficient in one engine may be awkward in another.' },
      { heading: '5. When MoE is attractive', content: 'MoE is attractive when a team wants high capability without activating all parameters for every token. It can work well for broad assistants, multilingual models, coding models, and systems that need a large knowledge or skill surface. The value is highest when serving infrastructure can handle the expert layout efficiently.' },
      { heading: '6. Risks and evaluation', content: 'MoE models can show uneven latency, expert imbalance, and surprising memory requirements. Evaluate with representative prompts across domains because routing behavior can vary by task. Measure not only average speed but p95 latency and GPU utilization. Also check whether quantization supports the MoE layers cleanly.' },
      { heading: '7. Comparison with dense models', content: 'Dense models are simpler to deploy and reason about because every token uses the same main parameter path. MoE models can be more efficient for capability, but they add routing and expert-management complexity. For small teams, a dense model may be easier unless the MoE model offers a clear quality or cost advantage on measured tasks.' },
      { heading: '8. Practical recommendation', content: 'Consider MoE when quality requirements exceed compact dense models and your runtime supports the architecture well. Do not assume active-parameter count equals memory requirement. Use the config fields for number of experts and active experts per token, then verify memory and latency on the target serving stack.' },
    ],
    faq: [
      { q: 'Does MoE mean only active experts are stored?', a: 'No. Active experts reduce compute per token, but resident memory can still include many or all experts.' },
      { q: 'Are MoE models always faster?', a: 'No. Routing, expert loading, communication, and runtime support determine speed.' },
      { q: 'What config fields identify MoE?', a: 'Look for fields such as num_experts, num_local_experts, or num_experts_per_tok.' },
    ],
    related: ['tensor-parallelism', 'what-is-vllm', 'kv-cache-optimization'],
  }),
];
